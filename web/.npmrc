# npm 仓库配置
registry=https://npm.weizhipin.com

# 防止依赖提升导致的幽灵依赖问题
shamefully-hoist=false

# 对等依赖检查（宽松模式，避免版本冲突导致安装失败）
strict-peer-dependencies=false

# workspace 协议支持
link-workspace-packages=true

# 锁文件配置
lockfile=true

# 自动安装对等依赖
auto-install-peers=true
